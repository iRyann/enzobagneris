# Services

Cette couche encapsule l'acces aux donnees pour preparer la migration vers Strapi.

- Les services utilisent des JSON locaux pour l'instant.
- L'interface reste stable pour basculer vers une API plus tard.
